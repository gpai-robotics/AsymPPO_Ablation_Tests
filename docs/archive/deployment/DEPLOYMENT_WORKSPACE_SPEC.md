# Deployment Workspace Spec

Status:

- historical companion note
- kept so older links do not break
- the canonical deployment doc is now:
  - `docs/DEPLOYMENT_PLAN.md`

The workspace contract, Sim2Sim contract, bundle metadata rules, and
deployment-surface ownership have been folded into `DEPLOYMENT_PLAN.md` so the
repo has one main deployment entry point instead of two overlapping ones.
