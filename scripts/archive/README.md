# Script Archive

This directory contains inactive adaptation workflows, legacy evaluation
tools, and superseded deployment runtimes.

Do not invoke archived scripts as current entrypoints. Their dependencies and
contracts are preserved for future recovery but are intentionally disconnected
from the active task registry and launchers.

Current deployment entrypoint:

```text
scripts/deploy/run_unitree_mjlab_sim_deploy.sh
```
